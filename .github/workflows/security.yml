name: Security

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: "41 6 * * 1"  # weekly, Monday 06:41 UTC

permissions:
  contents: read

concurrency:
  group: security-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pip-audit:
    name: Dependency vulnerability scan (pip-audit)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          python-version: "3.12"
      - run: uv sync --frozen --extra dev
      - name: Audit installed dependencies
        # Documented exception (P2 supply-chain policy — vulnerabilities are addressed OR
        # documented): PYSEC-2026-113 affects pyarrow < 23.0.1. pyarrow is an optional `parquet`
        # extra used only for local decimal128 columnar *output* (no untrusted Arrow-IPC/Parquet
        # deserialization of remote data), and the fix is outside our tested `>=15,<21` range.
        # Tracked for a pyarrow-cap bump; see docs/security-model.md (P2-D).
        # pip-audit is pinned so the vulnerability gate is reproducible (a new pip-audit release
        # cannot silently change or break the scan); bump deliberately alongside the other pins.
        run: uv run --with pip-audit==2.10.1 pip-audit --ignore-vuln PYSEC-2026-113

  secret-scan:
    name: Secret scan (gitleaks)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0  # scan the full history
          persist-credentials: false
        # v3 is the Node 24 runtime migration (v2 was Node 20, unsupported after GitHub's
        # 2026-06-16 cutoff). Free for this personal/public repo — no GITLEAKS_LICENSE required.
      - uses: gitleaks/gitleaks-action@e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e # v3.0.0
        env:
          GITHUB_TOKEN: ${{ github.token }}

  workflow-lint:
    name: Lint workflows (actionlint + zizmor + pinning)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          python-version: "3.12"
      - name: actionlint (pinned download script + release)
        env:
          ACTIONLINT_SHA: 03d0035246f3e81f36aed592ffb4bebf33a03106  # v1.7.7
          ACTIONLINT_VERSION: 1.7.7
        run: |
          curl -sSfL "https://raw.githubusercontent.com/rhysd/actionlint/${ACTIONLINT_SHA}/scripts/download-actionlint.bash" -o download-actionlint.bash
          bash download-actionlint.bash "${ACTIONLINT_VERSION}"
          ./actionlint -color
      - name: zizmor (workflow security audit)
        run: uvx zizmor@1.27.0 --offline .github/workflows
      - name: All actions pinned to a full SHA (project policy)
        run: python3 scripts/check_pinned_actions.py

  # NOTE (Ops follow-up): actions/dependency-review-action requires the repository's Dependency
  # Graph to be enabled (and GitHub Advanced Security on private repos). It is intentionally not
  # wired here yet because it errors ("Dependency review is not supported on this repository")
  # until that setting is turned on. Dependency CVEs are already covered by the pip-audit job
  # above and by Dependabot; re-add a `dependency-review` job once the Dependency Graph is enabled.
